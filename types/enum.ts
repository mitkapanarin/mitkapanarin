export enum TechStackEnum {
  FRONTEND = "frontend",
  BACKEND = "backend",
  DEVOPS = "devops",
  DESIGN = "design",
  DATABASE = "database",
  UNIT_TESTING = "unit test",
  APP_DEV = "app DEV",
  GAME_DEV = "game DEV",
}

export enum ThemeTypesEnum {
  DARK = "dark",
  LIGHT = "light",
}

export enum ProjectTileViewEnum {
  GRID = "grid",
  LIST = "list",
}
